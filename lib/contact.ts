/*
  The one address on the site.

  Privacy and support both point here rather than at privacy@ and support@,
  because two addresses means two mailboxes to keep alive and the failure mode
  is a bounce from the address printed in an App Store listing. One inbox,
  aliased if it ever needs splitting.

  It is a real requirement rather than a courtesy: App Store Connect asks for a
  support URL, and a support page with no way to reach anyone is the sort of
  thing App Review sends back.
*/
export const CONTACT_EMAIL = "hello@durus.space";
